# Python side of mlb_statcast.qmd.
# Install into whichever interpreter reticulate resolves to; check with
#   reticulate::py_config()
bambi>=0.15
pymc>=5.16
arviz>=0.18,<1.0
pandas>=2.0
numpy>=1.26
scipy>=1.11
statsmodels>=0.14
scikit-learn>=1.3
matplotlib>=3.7,<3.11

# Optional: a compiled NUTS implementation, several times faster on models this
# size. The notebook detects it and falls back to the PyMC sampler if absent.
nutpie>=0.13
